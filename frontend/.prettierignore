# Generated by Next.js on every `next dev` / `next build`; formatting it only creates churn.
next-env.d.ts
# Managed by `next dev` (agent rules block); keep byte-identical to what Next writes.
AGENTS.md
CLAUDE.md
